// AUTO-GENERATED by `bloom add tailwind-merge`
// NPM package  : tailwind-merge@3.6.0
// JS global    : tailwind_merge
// DO NOT EDIT  — run `bloom add tailwind-merge` to regenerate.
//
// ignore_for_file: non_constant_identifier_names, avoid_js_interop_library_on_non_web_platforms, unused_import

import 'dart:js_interop';
import 'package:web/web.dart' as web;

/// Dart 3.5+ JS Interop binding for the `tailwind-merge` NPM package.
///
/// This file was auto-generated from the package's TypeScript declarations
/// fetched from esm.sh / DefinitelyTyped.
///
/// The package namespace is bridged onto `window.tailwind_merge` via the bootstrap
/// script in `web/index.html`. Access members through the top-level [tailwind_merge] getter.
///
/// Usage:
///   tailwind_merge.<member>(...)
@JS('tailwind_merge')
external TailwindMerge get tailwind_merge;

@JS('tailwind_merge')
extension type TailwindMerge(JSObject _) implements JSObject {
  @JS('isAny')
  external JSAny? isAny([
    JSAny? a0,
    JSAny? a1,
    JSAny? a2,
    JSAny? a3,
    JSAny? a4,
    JSAny? a5,
  ]);
  @JS('isAnyNonArbitrary')
  external JSAny? isAnyNonArbitrary([
    JSAny? a0,
    JSAny? a1,
    JSAny? a2,
    JSAny? a3,
    JSAny? a4,
    JSAny? a5,
  ]);
  @JS('isArbitraryFamilyName')
  external JSAny? isArbitraryFamilyName([
    JSAny? a0,
    JSAny? a1,
    JSAny? a2,
    JSAny? a3,
    JSAny? a4,
    JSAny? a5,
  ]);
  @JS('isArbitraryImage')
  external JSAny? isArbitraryImage([
    JSAny? a0,
    JSAny? a1,
    JSAny? a2,
    JSAny? a3,
    JSAny? a4,
    JSAny? a5,
  ]);
  @JS('isArbitraryLength')
  external JSAny? isArbitraryLength([
    JSAny? a0,
    JSAny? a1,
    JSAny? a2,
    JSAny? a3,
    JSAny? a4,
    JSAny? a5,
  ]);
  @JS('isArbitraryNumber')
  external JSAny? isArbitraryNumber([
    JSAny? a0,
    JSAny? a1,
    JSAny? a2,
    JSAny? a3,
    JSAny? a4,
    JSAny? a5,
  ]);
  @JS('isArbitraryPosition')
  external JSAny? isArbitraryPosition([
    JSAny? a0,
    JSAny? a1,
    JSAny? a2,
    JSAny? a3,
    JSAny? a4,
    JSAny? a5,
  ]);
  @JS('isArbitraryShadow')
  external JSAny? isArbitraryShadow([
    JSAny? a0,
    JSAny? a1,
    JSAny? a2,
    JSAny? a3,
    JSAny? a4,
    JSAny? a5,
  ]);
  @JS('isArbitrarySize')
  external JSAny? isArbitrarySize([
    JSAny? a0,
    JSAny? a1,
    JSAny? a2,
    JSAny? a3,
    JSAny? a4,
    JSAny? a5,
  ]);
  @JS('isArbitraryValue')
  external JSAny? isArbitraryValue([
    JSAny? a0,
    JSAny? a1,
    JSAny? a2,
    JSAny? a3,
    JSAny? a4,
    JSAny? a5,
  ]);
  @JS('isArbitraryVariable')
  external JSAny? isArbitraryVariable([
    JSAny? a0,
    JSAny? a1,
    JSAny? a2,
    JSAny? a3,
    JSAny? a4,
    JSAny? a5,
  ]);
  @JS('isArbitraryVariableFamilyName')
  external JSAny? isArbitraryVariableFamilyName([
    JSAny? a0,
    JSAny? a1,
    JSAny? a2,
    JSAny? a3,
    JSAny? a4,
    JSAny? a5,
  ]);
  @JS('isArbitraryVariableImage')
  external JSAny? isArbitraryVariableImage([
    JSAny? a0,
    JSAny? a1,
    JSAny? a2,
    JSAny? a3,
    JSAny? a4,
    JSAny? a5,
  ]);
  @JS('isArbitraryVariableLength')
  external JSAny? isArbitraryVariableLength([
    JSAny? a0,
    JSAny? a1,
    JSAny? a2,
    JSAny? a3,
    JSAny? a4,
    JSAny? a5,
  ]);
  @JS('isArbitraryVariablePosition')
  external JSAny? isArbitraryVariablePosition([
    JSAny? a0,
    JSAny? a1,
    JSAny? a2,
    JSAny? a3,
    JSAny? a4,
    JSAny? a5,
  ]);
  @JS('isArbitraryVariableShadow')
  external JSAny? isArbitraryVariableShadow([
    JSAny? a0,
    JSAny? a1,
    JSAny? a2,
    JSAny? a3,
    JSAny? a4,
    JSAny? a5,
  ]);
  @JS('isArbitraryVariableSize')
  external JSAny? isArbitraryVariableSize([
    JSAny? a0,
    JSAny? a1,
    JSAny? a2,
    JSAny? a3,
    JSAny? a4,
    JSAny? a5,
  ]);
  @JS('isArbitraryVariableWeight')
  external JSAny? isArbitraryVariableWeight([
    JSAny? a0,
    JSAny? a1,
    JSAny? a2,
    JSAny? a3,
    JSAny? a4,
    JSAny? a5,
  ]);
  @JS('isArbitraryWeight')
  external JSAny? isArbitraryWeight([
    JSAny? a0,
    JSAny? a1,
    JSAny? a2,
    JSAny? a3,
    JSAny? a4,
    JSAny? a5,
  ]);
  @JS('isFraction')
  external JSAny? isFraction([
    JSAny? a0,
    JSAny? a1,
    JSAny? a2,
    JSAny? a3,
    JSAny? a4,
    JSAny? a5,
  ]);
  @JS('isInteger')
  external JSAny? isInteger([
    JSAny? a0,
    JSAny? a1,
    JSAny? a2,
    JSAny? a3,
    JSAny? a4,
    JSAny? a5,
  ]);
  @JS('isNamedContainerQuery')
  external JSAny? isNamedContainerQuery([
    JSAny? a0,
    JSAny? a1,
    JSAny? a2,
    JSAny? a3,
    JSAny? a4,
    JSAny? a5,
  ]);
  @JS('isNumber')
  external JSAny? isNumber([
    JSAny? a0,
    JSAny? a1,
    JSAny? a2,
    JSAny? a3,
    JSAny? a4,
    JSAny? a5,
  ]);
  @JS('isPercent')
  external JSAny? isPercent([
    JSAny? a0,
    JSAny? a1,
    JSAny? a2,
    JSAny? a3,
    JSAny? a4,
    JSAny? a5,
  ]);
  @JS('isTshirtSize')
  external JSAny? isTshirtSize([
    JSAny? a0,
    JSAny? a1,
    JSAny? a2,
    JSAny? a3,
    JSAny? a4,
    JSAny? a5,
  ]);
  @JS('createTailwindMerge')
  external JSAny? createTailwindMerge([
    JSAny? a0,
    JSAny? a1,
    JSAny? a2,
    JSAny? a3,
    JSAny? a4,
    JSAny? a5,
  ]);
  @JS('extendTailwindMerge')
  external JSAny? extendTailwindMerge([
    JSAny? a0,
    JSAny? a1,
    JSAny? a2,
    JSAny? a3,
    JSAny? a4,
    JSAny? a5,
  ]);
  @JS('fromTheme')
  external JSAny? fromTheme([
    JSAny? a0,
    JSAny? a1,
    JSAny? a2,
    JSAny? a3,
    JSAny? a4,
    JSAny? a5,
  ]);
  @JS('getDefaultConfig')
  external JSAny? getDefaultConfig([
    JSAny? a0,
    JSAny? a1,
    JSAny? a2,
    JSAny? a3,
    JSAny? a4,
    JSAny? a5,
  ]);
  @JS('mergeConfigs')
  external JSAny? mergeConfigs([
    JSAny? a0,
    JSAny? a1,
    JSAny? a2,
    JSAny? a3,
    JSAny? a4,
    JSAny? a5,
  ]);
  @JS('twJoin')
  external JSAny? twJoin([
    JSAny? a0,
    JSAny? a1,
    JSAny? a2,
    JSAny? a3,
    JSAny? a4,
    JSAny? a5,
  ]);
  @JS('twMerge')
  external JSAny? twMerge([
    JSAny? a0,
    JSAny? a1,
    JSAny? a2,
    JSAny? a3,
    JSAny? a4,
    JSAny? a5,
  ]);
  @JS('validators')
  external JSAny? validators([
    JSAny? a0,
    JSAny? a1,
    JSAny? a2,
    JSAny? a3,
    JSAny? a4,
    JSAny? a5,
  ]);
  /// Call a named method on the JS object form of this package.
  /// Note: this member maps to a literal JS property named `callMethod` on the
  /// target object, not a dynamic-dispatch-by-name call. It only works if the
  /// target object happens to define a method literally named `callMethod`.
  external JSAny? callMethod(String method, [JSArray<JSAny?>? args]);
}

