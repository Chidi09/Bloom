---
import DocsLayout from '../../../layouts/DocsLayout.astro';
import CodeBlock from '../../../components/common/CodeBlock.astro';
import { Terminal, ArrowRight, Zap, RefreshCw } from 'lucide-preact';

const devCmd = `bloom dev --port 8080 --qr`;
---

<DocsLayout
  title="bloom dev"
  description="Start the local developer server with instant hot reloading and wireless companion pairing."
  currentSlug="cli-dev"
  category="CLI Tooling"
  toc={[
    { id: 'usage', title: 'Interactive Developer Server' },
  ]}
>
  <div class="space-y-12">
    <!-- Header -->
    <div id="overview" class="space-y-4 border-b border-slate-200 dark:border-zinc-800 pb-8">
      <h1 class="text-4xl sm:text-5xl font-black tracking-tight text-slate-900 dark:text-white font-mono">
        bloom dev
      </h1>
      <p class="text-lg text-slate-600 dark:text-slate-400 leading-relaxed max-w-3xl">
        Runs your Bloom application with sub-second hot reload, AST file watcher, and wireless device connection.
      </p>
    </div>

    <!-- Section 1 -->
    <section id="usage" class="space-y-4">
      <h2 class="text-2xl font-bold text-slate-900 dark:text-white">
        Interactive Developer Server
      </h2>
      <CodeBlock code={devCmd} lang="bash" filename="Terminal" />
    </section>

    <!-- Next Page Footer -->
    <div class="pt-8 border-t border-slate-200 dark:border-zinc-800 flex justify-between">
      <a
        href="/docs/cli/create"
        class="inline-flex items-center gap-2 px-5 py-3 rounded-2xl bg-slate-100 dark:bg-zinc-950 text-slate-900 dark:text-white font-bold text-xs hover:bg-slate-200 dark:hover:bg-zinc-800 transition"
      >
        <span>Previous: bloom create</span>
      </a>
      <a
        href="/docs/cli/generate"
        class="inline-flex items-center gap-2 px-5 py-3 rounded-2xl bg-purple-600 text-white font-bold text-xs hover:bg-purple-500 transition shadow-lg"
      >
        <span>Next: bloom generate</span>
        <ArrowRight className="w-4 h-4" />
      </a>
    </div>
  </div>
</DocsLayout>
