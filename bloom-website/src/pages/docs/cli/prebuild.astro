---
import DocsLayout from '../../../layouts/DocsLayout.astro';
import CodeBlock from '../../../components/common/CodeBlock.astro';
import { Smartphone, ArrowRight, ShieldCheck, Terminal } from 'lucide-preact';

const prebuildCmd = `bloom prebuild --clean`;
---

<DocsLayout
  title="bloom prebuild"
  description="Idempotent native build generator syncing bloom.yaml into iOS and Android manifests."
  currentSlug="cli-prebuild"
  category="CLI Tooling"
  toc={[
    { id: 'usage', title: 'Prebuild Command' },
  ]}
>
  <div class="space-y-12">
    <!-- Header -->
    <div id="overview" class="space-y-4 border-b border-slate-200 dark:border-slate-800 pb-8">
      <h1 class="text-4xl sm:text-5xl font-black tracking-tight text-slate-900 dark:text-white font-mono">
        bloom prebuild
      </h1>
      <p class="text-lg text-slate-600 dark:text-slate-400 leading-relaxed max-w-3xl">
        Compiles your declarative <code class="font-mono text-purple-400">bloom.yaml</code> configuration into native iOS Info.plist and Android AndroidManifest.xml files idempotently.
      </p>
    </div>

    <!-- Section 1 -->
    <section id="usage" class="space-y-4">
      <h2 class="text-2xl font-bold text-slate-900 dark:text-white">
        Prebuild Command
      </h2>
      <CodeBlock code={prebuildCmd} lang="bash" filename="Terminal" />
    </section>

    <!-- Next Page Footer -->
    <div class="pt-8 border-t border-slate-200 dark:border-slate-800 flex justify-between">
      <a
        href="/docs/cli/generate"
        class="inline-flex items-center gap-2 px-5 py-3 rounded-2xl bg-slate-100 dark:bg-slate-900 text-slate-900 dark:text-white font-bold text-xs hover:bg-slate-200 dark:hover:bg-slate-800 transition"
      >
        <span>Previous: bloom generate</span>
      </a>
      <a
        href="/docs/cli/deploy"
        class="inline-flex items-center gap-2 px-5 py-3 rounded-2xl bg-purple-600 text-white font-bold text-xs hover:bg-purple-500 transition shadow-lg"
      >
        <span>Next: bloom deploy</span>
        <ArrowRight className="w-4 h-4" />
      </a>
    </div>
  </div>
</DocsLayout>
