---
import DocsLayout from '../../../layouts/DocsLayout.astro';
import CodeBlock from '../../../components/common/CodeBlock.astro';
import { Server, ArrowRight, ShieldCheck, Terminal } from 'lucide-preact';

const devServerApi = `// Dev Server Local Endpoints
// GET  http://localhost:8080/__bloom/health
// GET  http://localhost:8080/__bloom/routes
// POST http://localhost:8080/__bloom/hot-reload`;
---

<DocsLayout
  title="Dev Server & Local Engine"
  description="Local HTTP inspector server and hot reload orchestration engine."
  currentSlug="dev-server"
  category="Developer Experience"
  toc={[
    { id: 'endpoints', title: 'Local Inspector Endpoints' },
  ]}
>
  <div class="space-y-12">
    <!-- Header -->
    <div id="overview" class="space-y-4 border-b border-slate-200 dark:border-zinc-800 pb-8">
      <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-cyan-500/10 border border-cyan-500/20 text-cyan-400 text-xs font-mono font-bold">
        <Server className="w-3.5 h-3.5" />
        Local Engine
      </div>
      <h1 class="text-4xl sm:text-5xl font-black tracking-tight text-slate-900 dark:text-white">
        Dev Server & Local Engine
      </h1>
      <p class="text-lg text-slate-600 dark:text-slate-400 leading-relaxed max-w-3xl">
        The embedded dev server orchestrates AST file watchers, route graph regeneration, and browser inspector sockets.
      </p>
    </div>

    <!-- Section 1 -->
    <section id="endpoints" class="space-y-4">
      <h2 class="text-2xl font-bold text-slate-900 dark:text-white">
        Local Inspector Endpoints
      </h2>
      <CodeBlock code={devServerApi} lang="dart" filename="Dev Server Endpoints" />
    </section>

    <!-- Next Page Footer -->
    <div class="pt-8 border-t border-slate-200 dark:border-zinc-800 flex justify-between">
      <a
        href="/docs/dev-experience/bloom-go"
        class="inline-flex items-center gap-2 px-5 py-3 rounded-2xl bg-slate-100 dark:bg-zinc-950 text-slate-900 dark:text-white font-bold text-xs hover:bg-slate-200 dark:hover:bg-zinc-800 transition"
      >
        <span>Previous: Bloom Go Companion</span>
      </a>
      <a
        href="/docs/dev-experience/devtools-extensions"
        class="inline-flex items-center gap-2 px-5 py-3 rounded-2xl bg-purple-600 text-white font-bold text-xs hover:bg-purple-500 transition shadow-lg"
      >
        <span>Next: DevTools Extensions</span>
        <ArrowRight className="w-4 h-4" />
      </a>
    </div>
  </div>
</DocsLayout>
