name: bloom_social_feed
description: Bloom Official Social Feed Reference Application
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.27.0'

dependencies:
  flutter:
    sdk: flutter
  bloom_framework:
    path: ../../packages/bloom_framework

dev_dependencies:
  flutter_test:
    sdk: flutter
