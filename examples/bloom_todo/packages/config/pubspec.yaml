name: bloom_todo_config
description: >
  Compile-time environment configuration, feature flags, and app constants
  shared across every app in the bloom_todo workspace.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  envied: ^0.5.4

dev_dependencies:
  build_runner: ^2.4.0
  envied_generator: ^0.5.4
  lints: ^3.0.0
  test: ^1.24.0
