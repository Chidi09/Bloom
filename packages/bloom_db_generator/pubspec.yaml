name: bloom_db_generator
description: Code generator for bloom_db models.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  analyzer: ^6.4.1
  build: ^2.4.1
  source_gen: ^1.5.0
  bloom_db:
    path: ../bloom_db

dev_dependencies:
  build_runner: ^2.4.9
  build_test: ^2.2.2
  test: ^1.25.0
  lints: ^4.0.0
