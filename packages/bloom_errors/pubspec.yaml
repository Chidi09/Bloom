name: bloom_errors
description: Strongly-typed HTTP error hierarchy, sibling package error mapping, and dev/prod error-rendering middleware for Bloom.
version: 0.1.0
homepage: https://bloom.dev
repository: https://github.com/bloom-framework/bloom
issue_tracker: https://github.com/bloom-framework/bloom/issues
topics:
  - bloom
  - errors
  - http

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  bloom_framework: ^0.2.2

dev_dependencies:
  flutter_lints: ^5.0.0
  test: ^1.25.0
