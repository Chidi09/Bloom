/// Re-export shim for [BloomMiddleware] pipeline handlers from `package:bloom_server`.
///
/// Provides composable request/response middleware pipelines for authentication, CORS, compression, and error boundaries.
library;

export 'package:bloom_server/src/server/bloom_middleware.dart';

