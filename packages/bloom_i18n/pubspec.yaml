name: bloom_i18n
description: Internationalization, locale resolution, ICU message formatting, and date localization for Bloom applications.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  bloom_framework:
    path: ../bloom_framework
  intl: ^0.19.0
  meta: ^1.15.0

dev_dependencies:
  flutter_lints: ^5.0.0
  test: ^1.25.0
