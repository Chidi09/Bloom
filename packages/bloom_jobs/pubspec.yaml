name: bloom_jobs
description: Background job queue and worker system for Bloom server applications.
version: 0.1.1
repository: https://github.com/Chidi09/Bloom
issue_tracker: https://github.com/Chidi09/Bloom/issues
topics:
  - bloom
  - jobs
  - queue

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  meta: ^1.15.0

dev_dependencies:
  flutter_lints: ^5.0.0
  test: ^1.25.0
