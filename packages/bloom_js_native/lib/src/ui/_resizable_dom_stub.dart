/// VM/server stub for [resolveContainerRatio] — no DOM available, so drag
/// tracking is a no-op here. Real behavior comes from `_resizable_dom_web.dart`,
/// selected automatically when compiling for the browser.
double? resolveContainerRatio({
  required String containerId,
  required double clientX,
  required double clientY,
  required bool vertical,
}) =>
    null;
