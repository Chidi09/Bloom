name: bloom_security
description: Production-ready CORS, security headers, rate limiting, and WebSocket upgrade utilities for Bloom servers.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  bloom_framework:
    path: ../bloom_framework

dev_dependencies:
  flutter_lints: ^5.0.0
