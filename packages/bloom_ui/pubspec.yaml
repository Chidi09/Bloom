name: bloom_ui
description: Reusable, accessible, token-driven Flutter UI primitives inspired by shadcn/ui with zero runtime dependencies.
version: 0.2.0
repository: https://github.com/Chidi09/Bloom/tree/main/packages/bloom_ui
issue_tracker: https://github.com/Chidi09/Bloom/issues
topics:
  - ui
  - flutter
  - components
  - widgets
  - shadcn

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: '>=3.19.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
