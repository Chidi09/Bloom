name: bloom_validate
description: Declarative, strictly-typed request and DTO schema validation for Bloom API routes.
publish_to: 'none'

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  bloom_framework:
    path: ../bloom_framework

dev_dependencies:
  flutter_lints: ^5.0.0
  test: ^1.25.0
